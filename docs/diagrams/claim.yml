graph LR
  subgraph User
    U((User))
  end
  subgraph Metamask
    M((Metamask))
  end
  subgraph EmblemVault_Contract
    E((EmblemVault_Contract))
  end
  subgraph VaultHandler_Contract
    V((VaultHandler_Contract))
  end  
  subgraph Claimed_Contract
    C((Claimed_Contract))
  end

  U -- 1: Signature Request <--> M
  U -- 2: Claim Vault Request --> V
  V -- 3: Mark Claimed --> C
  V -- 4: Burn --> E
